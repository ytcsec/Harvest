{
  "name": "@harvest/circuits",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Groth16 circuit proving harvest capacity above a threshold without revealing it",
  "scripts": {
    "build": "node build.mjs && node build.mjs harvest_enrolment",
    "build:capacity": "node build.mjs",
    "build:enrolment": "node build.mjs harvest_enrolment",
    "prove": "node test/prove.mjs && node test/enrol.mjs",
    "prove:capacity": "node test/prove.mjs",
    "prove:enrolment": "node test/enrol.mjs"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.1.0",
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.5",
    "@harvest/sdk": "*"
  }
}
